import 'package:hooks_riverpod/hooks_riverpod.dart';

Future<void> initProviders(ProviderContainer container) async {
  //Initialize providers here these are top level providers
  // container.read(myProvider).init();
}
